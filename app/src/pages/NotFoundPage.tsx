import React from "react";
import Page from "@/ui/page/Page.tsx";

const NotFoundPage: React.FC = () => {
    return (
        <Page className={"flex items-center justify-center h-full"}>
            Not Found
        </Page>
    )
}

export default NotFoundPage;